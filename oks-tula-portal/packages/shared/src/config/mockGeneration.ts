/**
 * Конфигурация генерации ДЕМО-данных (Приложение C ТЗ).
 * Всё сгенерированное по этим параметрам помечается isMock=true и бейджем
 * «ДЕМО-ДАННЫЕ» в UI. Значения не являются сведениями из ЕИС/ПОС!
 */

/** Фиксированный seed генерации — цифры стабильны между запусками. */
export const MOCK_SEED = 20260913;

/**
 * Ориентиры стоимости строительства по отраслям, ₽/м² (для детерминированной
 * цены мок-контракта: цена = f(площадь, мощность, отрасль), округление до тыс. ₽).
 * Значения демонстрационные; боевые данные — только из ЕИС по соглашению
 * (docs/INTEGRATION_EIS.md, скрапинг запрещён §15.5).
 */
export const MOCK_PRICE_PER_M2_BY_INDUSTRY: Record<string, number> = {
  education: 95_000,
  health: 130_000,
  sport: 85_000,
  culture: 90_000,
  energy: 45_000,
  housing_utilities: 40_000,
  construction: 70_000,
  social_policy: 80_000,
  it_communications: 60_000,
  agriculture_ecology: 50_000,
  other: 60_000,
};

/** Если площади нет — цена от мощности, ₽ за единицу мощности (по коду единицы). */
export const MOCK_PRICE_PER_CAPACITY_UNIT: Record<string, number> = {
  places: 1_200_000,
  bed_places: 1_500_000,
  visits_per_shift: 1_400_000,
  apartments: 3_500_000,
  houses: 4_000_000,
  m3_per_day: 250_000,
  m3_per_hour: 400_000,
  kw: 90_000,
  mw: 9_000_000,
  gcal_per_hour: 12_000_000,
  km: 25_000_000,
  m2: 70_000,
  objects: 5_000_000,
  persons: 1_000_000,
};

/** Минимальная/максимальная цена мок-контракта, ₽ (здравый смысл). */
export const MOCK_PRICE_BOUNDS = { min: 500_000, max: 3_000_000_000 } as const;

/** Пометка для UI и БД у всех сгенерированных записей. */
export const MOCK_DISCLAIMER =
  'Демо-данные, сгенерированы детерминированно для демонстрации интерфейса; не являются сведениями из ЕИС/ПОС и требуют замены боевым источником.';
