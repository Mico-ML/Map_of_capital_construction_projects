{
  "name": "@oks/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Общие типы контрактов API, справочники и утилиты портала ОКС Тульской области",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.."
  }
}
